# BackEnd API with DB template

1. Search and Replace all <replace_me> values (docker-instructions.md &.env-template)
2. Rename `.env-template` to `.env`
3. Docker instructions
4. Database setup instructions


